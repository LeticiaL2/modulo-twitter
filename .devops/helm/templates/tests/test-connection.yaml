apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "painel-risco-api.fullname" . }}-test-connection"
  labels:
    {{- include "painel-risco-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "painel-risco-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
